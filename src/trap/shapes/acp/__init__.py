"""The ACP bridge: drive an agent that speaks the Agent Client Protocol for one case.

``connection`` is JSON-RPC over the agent's stdio, ``session`` is one case's
conversation, ``hints`` holds what tp knows about particular agents, and ``bridge`` is
``tp shape acp``."""
